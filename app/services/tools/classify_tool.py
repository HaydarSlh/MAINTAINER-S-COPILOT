"""LLM tool: classify an issue into bug/feature/docs/question.

Schema + dispatch into classification_service. Wraps failures as ToolFailure.
"""

# TODO: tool schema (name, description, params) + run(text)
