"""Embedding model for the RAG corpus.

The brief requires backing the embedding-model choice with a retrieval-quality
number against at least one alternative on the golden set (DECISIONS.md D4) —
i.e. proving it fits THIS corpus, not just its advertised benchmark.
"""

# TODO: embed(texts) for the chosen model + a benchmark harness vs alternative
