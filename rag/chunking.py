"""Chunking strategy — NOT naive fixed-size.

The naive baseline (fixed-size chunks) is what we must beat; the chosen
strategy (e.g. structure-aware / semantic) is justified with a golden-set
number vs that baseline in DECISIONS.md D5.
"""

# TODO: chunk(documents) -> chunks (+ the naive baseline for comparison)
