// Widget bootstrap: read widget_id from the iframe URL, fetch the public
// config from the api (/embed/config/{widget_id}), then mount <Widget/> with
// runtime theme. Theme is NOT hardcoded — it comes from the config.

// TODO: parse widget_id, fetch config, createRoot(#root).render(<Widget/>)
export {};
