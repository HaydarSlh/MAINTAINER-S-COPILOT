// Widget -> FastAPI api client. The widget neither knows nor cares that
// Streamlit exists; it only calls the shared backend (config + chat).

// TODO: fetchConfig(widgetId), streamChat(message)
export {};
