// The chat widget UI: collapsed bubble that expands into a chat panel with
// input box and streamed messages. Theme (primary color, position) applied
// from the runtime config. Tailwind or vanilla CSS — TBD.

// TODO: bubble + panel, streamed messages from api /chat, themed at runtime
export {};
