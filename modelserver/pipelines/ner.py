"""NER pipeline — extract code-shaped entities. Integration only.

Pre-trained model (e.g. spaCy) or rule-augmented. Targets function names,
file paths, error codes, identifiers that appear in issue text.
"""

# TODO: load_model(), extract(text) -> entities
