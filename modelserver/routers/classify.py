"""POST /classify — fine-tuned encoder issue classification.

Returns label (bug/feature/docs/question) + confidence. Backed by
pipelines/classifier.py. This is the DEPLOYED model per DECISIONS.md D3.
"""

# TODO: POST /classify {text} -> {label, confidence}
