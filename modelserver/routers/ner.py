"""POST /ner — extract code-shaped entities from issue text.

Returns entities (function names, file paths, error codes, etc.). Integration
only — backed by pipelines/ner.py.
"""

# TODO: POST /ner {text} -> {entities: [...]}
