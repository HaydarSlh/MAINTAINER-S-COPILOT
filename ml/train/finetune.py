"""Fine-tune a small encoder for issue classification.

Per the brief: track training with a real run logger (e.g. MLflow), document
and defend the freeze policy (DECISIONS.md D2). Saves the artifact + emits the
model card. Artifact is uploaded to MinIO; only the card is committed.
"""

# TODO: train(splits) with run logger; save artifact; call model_card.write()
