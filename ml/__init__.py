# DL track: offline data prep, fine-tuning, baselines, three-way comparison.
# NOT a runtime container — these are jobs run during the week. Outputs:
# model artifact (-> MinIO), model card (committed), comparison numbers
# (-> DECISIONS.md), and the classification golden set.
