"""LLM baseline on the SAME test split (prompt a frontier model to classify).

One of the three models in the comparison. Reports accuracy, macro-F1,
per-class F1, latency, cost — cost matters here for the deployment argument.
"""

# TODO: evaluate(test) -> metrics (uses prompts/llm_classifier_baseline.md)
