"""Classical ML baseline on the SAME splits (e.g. TF-IDF + LogReg/SVM).

One of the three models in the comparison. Reports accuracy, macro-F1,
per-class F1, latency, cost.
"""

# TODO: train(splits), evaluate(test) -> metrics
