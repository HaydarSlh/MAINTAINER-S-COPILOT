# nginx config for the demo host container. Serves demo/host/index.html as a
# tiny static site on a distinct origin so the origin-allowlisting demo is
# real (allowed host vs. a non-allowlisted one).

server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
}
