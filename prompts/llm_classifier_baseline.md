<!-- Prompt for the LLM classification baseline (ml/baselines/llm_baseline.py).
     Kept stable so the baseline number is reproducible across eval runs. -->

TODO: instruction to classify an issue into exactly one of
bug | feature | docs | question, output constrained to the label only.
