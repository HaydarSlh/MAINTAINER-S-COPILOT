<!-- System prompt for the single tool-calling LLM (the Maintainer's Copilot).
     ONE LLM that picks tools — not a workflow, not multi-agent. Version this
     file; changes are reviewed. Filled Thu. -->

TODO: define role (issue-triage copilot for a maintainer), available tools
(classify / ner / summarize / rag / write_memory), when to use each, memory
policy (write_memory is explicit only — never auto-write), and grounding rules
(cite RAG sources; say so and fall back if a tool fails).
